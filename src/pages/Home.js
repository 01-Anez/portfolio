import React from 'react';
import Carousel from '../components/carousel';
const Home = () => {
    return (
        <section>
            <Carousel />
            <h2>Inicio</h2>
            <p>Bienvenido a la página de inicio.</p>
        </section>
    );
};

export default Home;
